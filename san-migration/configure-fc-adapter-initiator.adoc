---
permalink: san-migration/configure-fc-adapter-initiator.html 
sidebar: sidebar 
keywords: online, migration, workflow 
summary: '啟動器模式用於將連接埠連接到磁帶磁碟機、磁帶庫或具有外部 LUN 匯入 (FLI) 的第三方儲存。' 
---
= 為啟動器模式配置 FC 適配器
:allow-uri-read: 
:icons: font
:imagesdir: ../media/


[role="lead"]
啟動器模式用於將連接埠連接到磁帶磁碟機、磁帶庫或具有外部 LUN 匯入 (FLI) 功能的第三方儲存。您需要將 FC 目標適配器轉換為啟動器模式才能將其用於 FLI。

.開始之前
* 必須將適配器上的 LIF 從其所屬的任何連接埠集中刪除。
* 在將實體連接埠的特性從目標變更為啟動器之前，必須遷移或銷毀使用要修改的實體連接埠的每個儲存虛擬機器 (SVM) 的所有 LIF。


.步驟
. 從適配器中刪除所有 LIF：
+
[source, cli]
----
network interface delete -vserver <SVM_name> -lif <lif_name>,<lif_name>
----
. 讓您的轉接器離線：
+
[source, cli]
----
network fcp adapter modify -node <node_name> -adapter <adapter_port> -status-admin down
----
+
如果轉接器沒有離線，您也可以從系統上對應的轉接器連接埠移除纜線。

. 將適配器從目標變更為啟動器：
+
[source, cli]
----
system hardware unified-connect modify -t initiator <adapter_port>
----
. 重新啟動託管您變更的適配器的節點。
. 驗證 FC 連接埠是否配置為適合您的配置的正確狀態：
+
[source, cli]
----
system hardware unified-connect show
----
. 使適配器重新連線：
+
[source, cli]
----
node run -node _node_name_ storage enable adapter <adapter_port>
----


.下一步是什麼？
使用ONTAP儲存的啟動器連接埠對外部陣列的目標連接埠進行區域劃分。
